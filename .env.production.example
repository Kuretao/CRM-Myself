# Required for docker compose production deployment. Never commit the real file.
POSTGRES_PASSWORD=use-a-long-random-password-here
JWT_SECRET=use-a-separate-random-secret-at-least-64-characters-long
NOVA_DOMAIN=nova.example.com
CORS_ORIGINS=https://nova.example.com
